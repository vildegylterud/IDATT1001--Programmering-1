import Java.util.Random

class RandomTall {

    Java.util.Random random = new java.util.Random(); // vi lager en tilfeldigtallgenerator

    Int tall = random.nextInt(10); //gir en verdi i intervallet (0, 9)

    Int antall = new int(10);  // Opprett en tabell

    for(int i = 0; i < 1000; i++) {
        return tall
    }



}